mod compressors;
mod dictionary;
mod helpers;
mod in_memory_dict;
mod indexer;
mod my_bk_tree;
mod positional_intersect;
mod query_parser;
mod scoring;
pub mod search_engine;
mod spimi;

fn main() -> Result<(), Box<dyn std::error::Error>> {
    Ok(())
}
